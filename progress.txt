# DeckPVP Progress Log
Started: Mon Mar 16 2026

## Codebase Patterns
- Card definitions in `packages/shared/src/cards.ts` are shared references — clone cards (structuredClone or spread) when building player decks
- `STARTER_DECKS[classId]` returns the starter deck array for any ClassId
- Use `pnpm --filter @deck-pvp/<package>` to run commands in a specific package
- Shared package is referenced via `workspace:*` and imported as `@deck-pvp/shared`
- Client uses Vite + React 19 + Tailwind v4 (`@tailwindcss/vite` plugin) + PixiJS v8
- Server uses tsx for dev (watch mode), Express + Socket.io
- TypeScript strict mode is enabled via `tsconfig.base.json` at root
- Root `pnpm dev` starts client and server concurrently with `--parallel -r`
- Client on port 5173, server on port 3001
- GameEngine is the authoritative game logic class in `packages/server/src/GameEngine.ts`
- GameEngine stores games in a Map keyed by gameId — use `engine.getGame(id)` to retrieve
- Server tests use vitest — run with `pnpm --filter @deck-pvp/server test`
- Class-specific HP values are in GameEngine's `CLASS_HP` constant (warrior=75, rogue=65, mage=60)

---

## 2026-03-16 - US-001
- Scaffolded pnpm workspace monorepo with packages/client, packages/server, packages/shared
- Client: Vite + React 19 + TypeScript + Tailwind CSS v4 + PixiJS v8 + @pixi/react + @pixi/particle-emitter + socket.io-client
- Server: Node.js + Express + TypeScript + Socket.io with health check at /health
- Shared: TypeScript package exporting GAME_CONFIG constants
- Landing page with PixiJS canvas rendering a purple rectangle to verify Pixi works
- Root `pnpm dev` starts both client (5173) and server (3001) concurrently
- All packages typecheck cleanly with strict mode
- Files changed: package.json, pnpm-workspace.yaml, tsconfig.base.json, .gitignore, packages/shared/*, packages/server/*, packages/client/*
- **Learnings for future iterations:**
  - @pixi/react v8 requires React 19+ (peer dep)
  - Tailwind v4 uses `@tailwindcss/vite` plugin and `@import "tailwindcss"` in CSS (no tailwind.config.js needed)
  - PixiJS v8 uses `new Application()` then `app.init({...})` (async) pattern
  - Server uses `tsx watch` for hot-reload during development
---

## 2026-03-16 - US-002
- Defined shared TypeScript types: Card, Effect, PlayerState, GameState, TurnPhase, and related union types
- Created `packages/shared/src/types.ts` with all type definitions
- Updated `packages/shared/src/index.ts` to re-export types
- All packages typecheck cleanly
- Files changed: packages/shared/src/types.ts (new), packages/shared/src/index.ts
- **Learnings for future iterations:**
  - Shared types are exported via `export * from './types.js'` (`.js` extension needed for ESM resolution with bundler moduleResolution)
  - GameState.players is a tuple `[PlayerState, PlayerState]` for exactly 2 players
  - Card.class uses `ClassId` type ('warrior' | 'rogue' | 'mage') matching the gameDesign.classes in PRD
---

## 2026-03-16 - US-003
- Defined card data for all 3 starter classes in `packages/shared/src/cards.ts`
- Warrior: 5x Strike (1e, 6 dmg), 4x Defend (1e, 5 block), 1x Bash (2e, 8 dmg + 2 str)
- Rogue: 5x Shiv Strike (0e, 3 dmg), 3x Defend (1e, 5 block), 1x Poison Blade (1e, 3 dmg + 3 poison), 1x Dodge (1e, 7 block + draw 1)
- Mage: 4x Arcane Bolt (1e, 5 dmg), 3x Barrier (1e, 6 block), 1x Channel Lightning (1e, channel lightning orb), 1x Frost Shield (1e, channel frost orb + 3 block), 1x Surge (0e, +1 energy)
- Added `channel` EffectType and `OrbType` to shared types for Mage orb mechanic
- Exported `STARTER_DECKS` record and individual deck arrays (`WARRIOR_STARTER_DECK`, etc.)
- Files changed: packages/shared/src/types.ts, packages/shared/src/cards.ts (new), packages/shared/src/index.ts
- **Learnings for future iterations:**
  - Card definitions reference card objects by variable, so duplicate cards in a deck point to the same object — future GameEngine should clone cards when building a player's deck
  - `STARTER_DECKS` is keyed by ClassId string for easy lookup: `STARTER_DECKS[classId]`
  - Mage orb mechanic uses `orbType` optional field on Effect — only present when `type: 'channel'`
---

## 2026-03-16 - US-004
- Implemented GameEngine class in `packages/server/src/GameEngine.ts`
- Added `Orb` interface and `orbs: Orb[]` field to `PlayerState` in shared types
- GameEngine methods: `createGame`, `playCard`, `endTurn`, `getGame`
- Effect resolution: damage (with block absorption), block, strength (adds to attack damage), poison (ticks at start of turn), draw, energy, channel (orbs)
- Mage orbs: lightning deals 3 damage at end of turn, frost gives 3 block at start of turn
- Win detection on HP reaching 0 (from attacks, poison, or lightning orbs)
- Draw pile reshuffles from discard when empty
- 21 unit tests covering all acceptance criteria (vitest)
- Files changed: packages/shared/src/types.ts, packages/server/src/GameEngine.ts (new), packages/server/src/GameEngine.test.ts (new), packages/server/package.json
- **Learnings for future iterations:**
  - Cards are cloned with unique IDs (`{cardId}-{index}`) when building player decks so duplicate cards can be distinguished
  - `crypto.randomUUID()` works in Node.js 18+ without imports
  - vitest works out of the box with TypeScript ESM (no config needed for server package)
  - Player's initial hand from createGame persists into their first turn — endTurn only discards the *current* player's hand, not the next player's
  - Class-specific HP: warrior=75, rogue=65, mage=60 (defined in GameEngine, not shared config)
---
